// Video 05 -- Les enums et tuples  ----------------

// Enum
enum Weekday {
    lundi, 
    mardi,
    mercredi
}

console.log(Weekday[1]);

// Tuples

let identity: [string, number] = [ "Alexis", 26];

console.log(identity)

